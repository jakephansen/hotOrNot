# Checks mode passed into function
def errorCheckMode(mode):
	if mode == "small":
		return
	elif mode == "large":
		return
	else:
		print "Please enter either 'small' or 'large' as an argument for the program"
		exit(1)

